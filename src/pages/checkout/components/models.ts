interface CheckoutProductCardProps {
  productName: string;
  brand: string;
  thumbnail: string;
  quantity: number;
  price: number;
}

interface UserCardProps {
  firstName: string;
  lastName: string;
  address: string;
  city: string;
}
